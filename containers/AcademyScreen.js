import React from "react";
import { Text, View, TouchableOpacity, StyleSheet, Image } from "react-native";

const AcademyScreen = ({ navigation }) => {
  <Text>Rejoins l'académie des givrés !!</Text>;
};

export default AcademyScreen;
